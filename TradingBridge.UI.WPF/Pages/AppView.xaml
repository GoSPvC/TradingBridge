<Window x:Class="TradingBridge.UI.WPF.Pages.AppView"
        
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        xmlns:local="clr-namespace:TradingBridge.UI.WPF.Pages"
        xmlns:home="clr-namespace:TradingBridge.UI.WPF.Pages.Home"
        xmlns:namedPipe="clr-namespace:TradingBridge.UI.WPF.Pages.NamedPipeServer"

        d:DataContext="{d:DesignInstance Type=local:AppViewModel}"
        mc:Ignorable="d"
        
        Title="TradingBridge"
        Height="600"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6FA">

    <Window.Resources>
        <!-- DataTemplates for View-ViewModel mapping -->
        <DataTemplate DataType="{x:Type home:HomeViewModel}">
            <home:HomeView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type namedPipe:NamedPipeServerViewModel}">
            <namedPipe:NamedPipeServerView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Sidebar -->
        <Border Grid.Column="0" 
        Background="#2C3E50"
        BorderThickness="0,0,1,0"
        BorderBrush="#1A252F">
            <StackPanel Margin="0,20,0,0">
                <!-- App Title -->
                <TextBlock Text="ExampleApp"
                  FontSize="20"
                  FontWeight="Bold"
                  Foreground="White"
                  HorizontalAlignment="Center"
                  Margin="0,0,0,30"/>

                <!-- Navigation Buttons -->
                <Button Content="🏠 Home"
                Command="{Binding NavigateToHomeCommand}"
                Style="{StaticResource NavigationButtonStyle}"
                Margin="10,5"/>

                <Button Content="🔌 Named Pipe Server"
                Command="{Binding NavigateToNamedPipeServerCommand}"
                Style="{StaticResource NavigationButtonStyle}"
                Margin="10,5"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Border Grid.Column="1" 
        Background="White"
        Margin="10">
            <ContentControl Content="{Binding CurrentPageViewModel}"/>
        </Border>
    </Grid>

</Window>
